# Copy this file to .env and fill in your key to enable full LLM tool-calling mode.
# Without this key, the API still works fully via the rule-based fallback path.
ANTHROPIC_API_KEY=sk-ant-your-key-here
